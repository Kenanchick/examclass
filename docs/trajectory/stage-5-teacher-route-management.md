# Этап 5. Управление маршрутом преподавателем

## Граница этапа

Реализовано рабочее место преподавателя для просмотра и ручного управления
персональным маршрутом ученика. Диагностика, профиль и алгоритм маршрута не
заменены: преподаватель работает поверх их объяснимых результатов.

Принципиально разделены три состояния:

1. системная оценка владения навыком;
2. факт прохождения материала с преподавателем;
3. ручное педагогическое решение о маршруте.

Отметка «пройдено на занятии» не переводит навык в «освоено». Система ожидает
подтверждение практикой или контрольной работой.

## Что видит преподаватель

Для каждого ученика доступны:

- текущий учебный маршрут и его блокировки;
- системный статус каждого навыка;
- владение, уверенность, скорость и стабильность;
- объяснение причин статуса;
- обязательные пробелы, блокирующие следующие навыки;
- задачи, попытки и источники доказательств;
- история пересчётов профиля;
- история ручных педагогических решений;
- темы, требующие контроля или повторения.

Профиль из всех навыков находится рядом с коротким маршрутом. Преподаватель
может исследовать полную картину, но ученик по-прежнему получает только
понятные учебные модули.

## Действия с навыком

Преподаватель может:

- подтвердить вывод системы;
- установить ручной статус и временно отключить автоматику;
- вернуть навык под автоматическую оценку;
- отметить материал пройденным;
- отметить материал закреплённым на занятии;
- назначить контроль;
- поставить навык на повторение;
- сохранить педагогический комментарий.

`TeacherSkillControl` хранит текущую педагогическую настройку. Она не
перезаписывает `StudentSkillState`, поэтому исходный системный вывод всегда
можно показать и вернуть.

При отключении автоматики текущий системный статус фиксируется как начальный
ручной статус. При включении автоматики ручной статус очищается, и снова
используется пересчитываемая оценка профиля.

## Действия с модулем

Для модуля маршрута доступны:

- перемещение выше или ниже;
- закрепление позиции;
- временное скрытие;
- отключение автоматического изменения;
- комментарий;
- добавление собственной темы.

При следующем пересчёте автоматические модули обновляются алгоритмом. Ручной
порядок, закреплённые, скрытые, пользовательские и отключённые от автоматики
модули переносятся в новый снимок маршрута.

Недельная нагрузка изменяется отдельно. После изменения создаётся новая версия
маршрута с новым доступным бюджетом времени.

## Неизменяемая история

Каждое ручное действие создаёт `TeacherRouteChange`:

- автор;
- ученик;
- маршрут;
- навык или модуль;
- тип действия;
- обязательная причина;
- состояние до изменения;
- состояние после изменения;
- точное время.

История не редактируется при следующих изменениях. Текущее состояние хранится в
`TeacherSkillControl` и `LearningRouteModule`, а журнал позволяет восстановить,
кто и почему принял решение.

Автоматические пересчёты профиля отдельно создают
`StudentSkillStateRevision`. Поэтому преподаватель видит изменение системной
оценки независимо от ручной истории.

## Модели

`TeacherSkillControl`

- педагогический статус `NOT_STARTED`, `TAUGHT` или `REINFORCED`;
- ручной статус навыка;
- признак автоматического пересчёта;
- даты подтверждения вывода, контроля и повторения;
- комментарий и последний автор.

`TeacherRouteChange`

- неизменяемое событие ручного решения;
- снимки `before` и `after`;
- причина, автор и время.

`StudentSkillStateRevision`

- снимок показателей после каждого пересчёта профиля;
- версия формулы и пакет расчёта.

`LearningRouteModule`

- флаги закрепления, скрытия, пользовательского происхождения;
- блокировка автоматического изменения;
- ручной комментарий и зафиксированная позиция.

## API

Для ученика своего класса преподаватель использует:

- `GET /teacher/learning-routes/students/:studentId`;
- `POST /teacher/learning-routes/students/:studentId/rebuild`;
- `GET /teacher/learning-routes/students/:studentId/history`;
- `GET /teacher/learning-routes/students/:studentId/skills/:skillCode`;
- `POST /teacher/learning-routes/students/:studentId/skills/:skillCode/actions`;
- `POST /teacher/learning-routes/students/:studentId/modules/:moduleKey/actions`;
- `POST /teacher/learning-routes/students/:studentId/modules`;
- `PATCH /teacher/learning-routes/students/:studentId/goal`.

Доступ проверяется общей службой: преподаватель может управлять только
учеником, который состоит в его классе.

## Интерфейс

На странице ученика преподаватель видит:

- маршрут в порядке выполнения;
- краткие причины и блокировки каждого модуля;
- поиск и фильтр по полному профилю;
- подробную панель выбранного навыка;
- форму собственной темы;
- журнал ручных действий;
- настройку недельной нагрузки.

TanStack Query отвечает за серверный маршрут, профиль, детали и мутации.
Zustand хранит только локальное состояние рабочего места: выбранный навык,
поиск, фильтр и режим показа скрытых модулей.

Любое изменяющее действие открывает подтверждение и требует причину. Это не
позволяет создавать неинформативные события аудита.

## Детерминированность

LLM не участвует в вычислении статусов, разблокировке и переносе ручных
настроек. Эффективный статус выбирается прозрачным правилом:

- при включённой автоматике используется `StudentSkillState`;
- при отключённой автоматике используется ручной статус;
- «пройдено» и «закреплено с преподавателем» показываются отдельно;
- назначенное повторение и контроль влияют на тип следующего модуля, но не
  подменяют доказательство освоения.

## Проверка

Добавлены доменные тесты разделения системного и педагогического состояния:

- прохождение с преподавателем без автоматического освоения;
- применение зафиксированного ручного статуса;
- возврат к системной оценке;
- отдельное планирование повторения;
- приоритет контрольного модуля.

Для локальной проверки предусмотрен воспроизводимый демонстрационный маршрут:

```bash
pnpm --filter @examclass/api run prisma:seed-learning-route-demo
```

Он создаёт профиль и маршрут для `demo@examclass.local`, не изменяя карту
знаний и правила алгоритма.

## Ограничения первой версии

- Проверка развёрнутых решений остаётся источником evidence из этапов 2–3 и не
  переносится в интерфейс маршрута.
- Изменение порядка действует внутри текущего маршрута и переносится в
  следующий снимок; полноценный календарный план занятий будет отдельным
  этапом.
- Даты контроля и повторения пока задаются действием на текущий момент; выбор
  конкретного времени можно добавить вместе с календарным планированием.
- Коэффициенты профиля и маршрута требуют калибровки на реальных данных, но
  ручные решения уже полностью объяснимы и аудируемы.
