# Этап 4. Персональный учебный маршрут

## Граница этапа

На этом этапе реализованы:

- детерминированный алгоритм формирования маршрута;
- группировка навыков в понятные учебные модули;
- обязательная проверка зависимостей;
- правила завершения и разблокировки;
- учёт цели, срока, недельного бюджета, повторений и назначений преподавателя;
- версионированное хранение маршрута и API чтения/пересчёта.

Интерактивная визуальная карта в этот этап не входит.

## Источники данных

Алгоритм не копирует карту знаний или профиль. Он читает:

- `KnowledgeNode` — фундаментальность, важность, сложность и время освоения;
- `KnowledgeDependency` — обязательные и рекомендуемые зависимости;
- `KnowledgeExamMapping` — номера, части и вес навыка на экзамене;
- `StudentSkillState` — уровень, уверенность, стабильность и повторение;
- `StudentLearningGoal` — целевой балл, дату экзамена и минуты в неделю;
- активные `HomeworkAssignment` — навыки, уже назначенные преподавателем.

Результат сохраняется как пересчитываемая проекция `LearningRoute`. Предыдущий
активный маршрут получает статус `SUPERSEDED`, поэтому история решений не
теряется.

## Почему маршрут не является списком слабых навыков

Построение проходит последовательно:

1. Из профиля выбираются значимые пробелы, повторения, нестабильные навыки и
   назначения преподавателя.
2. Для выбранных навыков рекурсивно добавляется вся неосвоенная цепочка
   обязательных предпосылок.
3. При достаточном времени добавляются до двух полезных рекомендуемых опор.
4. Каждый навык получает объяснимый приоритет.
5. Навыки группируются по учебным подтемам и типу действия.
6. Модули сортируются топологически: обязательная база всегда оказывается
   раньше зависимого модуля.
7. Маршрут ограничивается горизонтом до шести недель и максимум двенадцатью
   модулями.

Например, назначенные преподавателем логарифмические уравнения останутся в
маршруте, но получат статус `BLOCKED`, если сначала нужно закрыть арифметику,
дроби или преобразования выражений.

## Приоритет

Для каждого навыка сохраняются все факторы:

```text
priority =
  knowledgeGap
  × targetRelevance
  × examImpact
  × foundationalReach
  × deadlineUrgency
  × timeEfficiency
  × confidenceReliability
  + reviewBonus
  + teacherBonus
```

### Пробел

- явное «не изучал» даёт максимальную потребность в освоении;
- слабый или изучаемый навык получает вес по `1 - mastery`;
- повторение имеет отдельный бонус;
- недостаточная уверенность не трактуется как доказанный пробел.

### Целевой балл

- до 70 баллов сильнее приоритизируется первая часть;
- от 70 до 89 вклад второй части повышается;
- при цели 90+ вторая часть получает максимальный вес.

Первая часть не обесценивается даже для сильного ученика: случайные потери в ней
по-прежнему влияют на итоговый балл.

### Экзаменационный вклад

Используются:

- важность навыка из карты;
- число разных номеров ЕГЭ;
- вес связи навыка с номером.

### Фундаментальность

Учитываются флаг `isFoundational` и число последующих навыков, доступ к которым
открывает текущий навык. Обязательные связи дают полный вклад, рекомендуемые —
половинный.

### Срок и доступное время

Чем ближе экзамен, тем выше срочность. Оценочное время навыка влияет на
эффективность выбора. Недельный бюджет определяет:

- доступное число минут в горизонте;
- число модулей, которые можно показать;
- возможность вести независимый модуль параллельно.

## Типы модулей

| Тип                | Когда создаётся                                                    |
| ------------------ | ------------------------------------------------------------------ |
| `REQUIRED`         | Обязательная база, фундаментальный или целевой неосвоенный навык   |
| `RECOMMENDED`      | Полезный модуль с меньшим влиянием на текущую цель                 |
| `PARALLEL`         | Независимый рекомендуемый модуль, который можно вести одновременно |
| `CONTROL`          | Уровень высокий, но результаты нестабильны                         |
| `REVIEW`           | Наступил срок интервального повторения                             |
| `EXTRA_DIAGNOSTIC` | Уверенность ниже 0,45 или меньше двух независимых подтверждений    |
| `TEACHER_ASSIGNED` | Навык входит в активное домашнее задание преподавателя             |

Назначение преподавателя повышает приоритет, но не отменяет обязательные
предпосылки.

## Низкая уверенность

Если данных мало, система создаёт `EXTRA_DIAGNOSTIC` с целью:

- получить минимум две независимые проверки;
- поднять уверенность не ниже 0,55.

У такого модуля нет целевого `mastery`: он не утверждает, что тему нужно
изучать, пока причина неопределённости не проверена.

Статус `UNSTUDIED` обрабатывается иначе. Это явный сигнал, поэтому модуль
назначается на освоение, а не на повторную общую диагностику.

## Завершение и разблокировка

Обязательная предпосылка считается готовой, если:

- `mastery >= 0,72`;
- `confidence >= 0,50`;
- повторение не просрочено;

либо навык подтверждён преподавателем.

Зависимый модуль до этого имеет статус `BLOCKED` и возвращает точные
`blockedBySkillCodes`.

Критерии завершения сохраняются отдельно у каждого модуля:

- дополнительная диагностика — две независимые проверки и confidence `0,55`;
- контроль — mastery `0,80`, confidence `0,65`, stability `0,70`;
- повторение — новое успешное воспроизведение после срока повторения;
- освоение — по всем навыкам mastery не ниже `0,75`, confidence `0,55`,
  stability `0,55`;
- для фундаментального навыка целевой mastery повышается до `0,80`.

После появления нового evidence профиль меняется. Текущий маршрут помечается в
API как `isStale`, а пересчёт создаёт новый снимок с актуальными статусами.

## Автоматическое формирование

Маршрут автоматически строится:

- после завершения диагностики без ручной проверки;
- после проверки преподавателем последнего задания с развёрнутым ответом.

Его также можно пересчитать явно.

## API

Ученик:

- `GET /learning-routes/current`;
- `POST /learning-routes/rebuild`.

Преподаватель для ученика своего класса:

- `GET /teacher/learning-routes/students/:studentId`;
- `POST /teacher/learning-routes/students/:studentId/rebuild`.

Ответ содержит только выбранные учебные модули, а не внутреннюю карту из всех
навыков. Внутри модуля возвращаются конкретные навыки, текущие показатели,
критерии завершения, блокировки, факторы и причины выбора.

## Проверочные сценарии

Автоматические тесты покрывают:

1. пробел в базовой арифметике перед сложной темой;
2. освоенную первую часть и слабую вторую;
3. сильного, но нестабильного ученика;
4. явно не пройденную часть программы;
5. цель около 70 баллов;
6. цель 90+;
7. недостаток диагностических данных;
8. повторение и назначение преподавателя;
9. независимый параллельный модуль.

## Ограничения первой версии

- Коэффициенты целевого балла и экзаменационного вклада требуют последующей
  калибровки на реальных результатах.
- Назначения преподавателя сейчас извлекаются из активных домашних заданий.
- Интерфейс ручного перемещения и замены модулей будет отдельным этапом.
- Алгоритм не использует LLM и при одинаковом наборе данных даёт одинаковый
  результат.
