# Этап 1. Полная карта знаний по профильной математике

## Итоговая концепция

Карта знаний отделена от существующего каталога задач и от будущего состояния конкретного ученика.

```text
Раздел → Тема → Подтема → Проверяемый навык
```

- `Topic` в существующей базе продолжает группировать задачи банка.
- `KnowledgeNode` описывает структуру знаний и содержит узлы четырёх уровней.
- только узел `SKILL` является единицей проверки и будущей оценки знаний;
- `KnowledgeDependency` задаёт обязательные (`REQUIRED`) и рекомендуемые (`RECOMMENDED`) предпосылки;
- `TaskSkill` позволяет одной задаче проверять несколько навыков с разной ролью и весом;
- `KnowledgeExamMapping` связывает навык с номерами и типами заданий ЕГЭ;
- материалы связаны с навыками через постраничные сегменты, а не через название файла.

Карта имеет версию и статус `IN_REVIEW`: спорные решения нельзя незаметно выдать за окончательную экспертную разметку.

## Объём карты

| Сущность                         | Количество |
| -------------------------------- | ---------: |
| Разделы                          |          9 |
| Темы                             |         18 |
| Подтемы                          |         34 |
| Проверяемые навыки               |        150 |
| Обязательные зависимости         |        281 |
| Рекомендуемые зависимости        |         17 |
| Теоретические материалы          |         18 |
| Постраничные сегменты материалов |         42 |
| Пробелы покрытия                 |          9 |
| Группы вопросов эксперту         |          6 |

Покрыты все номера 1–19 профильного ЕГЭ. Номер задания не используется как тема: один навык может участвовать в нескольких номерах, а одна задача может ссылаться на несколько навыков.

## Нормализация

Повторяющиеся формулы из разных PDF сведены к одним и тем же навыкам. Например:

- «формулы сокращённого умножения» и «разложение на множители» разделены на применение тождества и выбор способа факторизации;
- «степени и корни» разделены на свойства степеней, арифметический корень, рационализацию и преобразование степенных выражений;
- «логарифмы» разделены на определение и ОДЗ, свойства, преобразование выражений, уравнения и неравенства;
- «тригонометрия» является темой, внутри которой выделены единичная окружность, значения функций, тождества, преобразования, уравнения и отбор корней;
- «планиметрия» и «стереометрия» не считаются навыками: проверяются конкретные построения, вычисления, доказательства и пространственные отношения;
- «задачи с параметром» представлены разбором случаев и зависимостями от уравнений, неравенств и графиков.

Навык считается достаточно узким, если его можно проверить отдельным заданием или наблюдаемым шагом решения, но достаточно содержательным, чтобы по результату можно было назначить учебное действие.

## Фундаментальные навыки

Флаг `isFoundational` получили навыки с большим числом последующих применений:

- действия со знаковыми числами, дробями, процентами и пропорциями;
- порядок действий, перевод единиц и оценка правдоподобия результата;
- свойства степеней и корней;
- раскрытие скобок, приведение подобных, формулы сокращённого умножения и факторизация;
- линейные и квадратные уравнения, равносильность преобразований;
- чтение графика функции и координатная плоскость;
- базовые свойства треугольников, подобие и теорема Пифагора;
- структура математического доказательства и контроль ограничений.

Обязательная зависимость означает, что без предпосылки устойчивое освоение следующего навыка маловероятно. Рекомендуемая связь повышает качество или скорость освоения, но не блокирует его.

## Метаданные навыка

Для каждого из 150 навыков заданы:

- стабильный код, название и проверяемое описание;
- сложность от 1 до 5;
- важность от 1 до 5;
- ориентировочное время освоения в минутах;
- номера ЕГЭ и типы задач;
- один или несколько способов проверки;
- обязательные и рекомендуемые зависимости;
- признак фундаментальности;
- полнота покрытия исходными материалами;
- необходимость экспертной проверки и пояснение.

Способы проверки включают короткий ответ, многошаговое решение, устное объяснение, анализ ошибки, чтение графика, построение, доказательство и математическое моделирование.

## Что обнаружено в материалах

Названия PDF не образуют корректную таксономию. Материалы к отдельным номерам содержат общие формулы, нужные сразу в нескольких частях экзамена. Поэтому все 18 файлов были просмотрены постранично и разбиты на 42 смысловых сегмента.

Материалы хорошо поддерживают:

- базовые алгебраические тождества, степени, корни и логарифмы;
- планиметрию и значительную часть стереометрии;
- основные графики и производную;
- типовые текстовые задачи;
- базовую вероятность;
- делимость, прогрессии и элементы задач №19.

При этом это преимущественно краткие формульные памятки. Они не являются полным учебным курсом: почти нет объяснения причин, систем упражнений, типичных ошибок и доказательств.

## Пробелы материалов

| Приоритет | Пробел                                                                            |
| --------- | --------------------------------------------------------------------------------- |
| Высокий   | Вычислительный фундамент: знаки, дроби, порядок действий, единицы и оценка ответа |
| Высокий   | Тригонометрические уравнения и отбор корней                                       |
| Высокий   | Условная вероятность, формула полной вероятности, Бернулли и комбинаторика        |
| Высокий   | Финансовые модели №16                                                             |
| Высокий   | Системный набор методов задач с параметром №18                                    |
| Высокий   | Стратегии доказательства и дополнительных построений в №14 и №17                  |
| Средний   | Глубокая работа с функциями, производной и оптимизацией                           |
| Средний   | Координатно-векторный метод в стереометрии                                        |
| Низкий    | Элементы статистики, частоты и первообразной                                      |

Эти ветви присутствуют в карте, но помечены `MISSING` или `PARTIAL`: структура знаний не должна искусственно сужаться до содержания имеющихся памяток.

## Что должен проверить преподаватель

1. Калибровку сложности, важности и времени освоения.
2. Достаточность декомпозиции аналитического и графического методов задач с параметром.
3. Гранулярность дополнительных построений и доказательств в сложной геометрии.
4. Актуальный набор финансовых схем №16.
5. Фактический вес статистики и первообразной в текущей экзаменационной модели.
6. Право использования PDF и формат их дальнейшего хранения в продукте.

## Реализация

- декларативная карта и валидатор: `apps/api/prisma/knowledge-map.data.ts`, `knowledge-map.catalog.ts`;
- модели базы: `apps/api/prisma/schema.prisma`;
- миграция: `apps/api/prisma/migrations/20260725143000_add_knowledge_map/migration.sql`;
- повторяемый импорт: `apps/api/prisma/knowledge-map.seed.ts`;
- read-only API:
  - `GET /knowledge-maps/profile-math-ege`;
  - `GET /knowledge-maps/profile-math-ege/skills/:skillCode`;
- автоматические проверки целостности: `apps/api/src/modules/knowledge-map/*.spec.ts`.

В базе сохраняются только метаданные предоставленных PDF и постраничные связи. Сами PDF не копируются в репозиторий.

## Граница этапа

Не реализованы и не моделируются результатами этого этапа:

- диагностика ученика;
- профиль освоения навыков;
- адаптивные уточняющие вопросы;
- персональный маршрут;
- история состояния ученика;
- расписание повторений;
- интерфейс дорожной карты.

Они должны опираться на опубликованную и проверенную версию карты, но разрабатываются отдельно.
