# Этап 3. Профиль знаний ученика

## Граница этапа

Профиль использует карту знаний и evidence, полученные на этапах 1–2. На этом этапе реализованы расчёт состояния каждого навыка, объяснения для преподавателя и API чтения профиля.

Не реализованы:

- персональный учебный маршрут;
- визуальная дорожная карта;
- автоматическое планирование занятий;
- интерфейс профиля.

## Разделение данных

`SkillEvidence` — неизменяемый факт отдельной попытки. Он хранит:

- результат от 0 до 1;
- базовый вес и роль навыка в задании;
- сложность;
- источник;
- самостоятельность;
- активное и ожидаемое время;
- уверенность ученика;
- тип ошибки;
- подтверждение преподавателем;
- дату и ключ независимости попытки.

`StudentSkillState` — пересчитываемая проекция. В ней находятся текущие показатели, статус, срок повторения, сводка источников и объяснение расчёта.

Таким образом, изменение формулы не требует переписывать историю: проекцию можно восстановить из evidence.

## Показатели навыка

Для каждого навыка рассчитываются:

- `mastery` — предполагаемый уровень владения от 0 до 1;
- `confidence` — уверенность системы от 0 до 1;
- `speed` — скорость успешного применения от 0 до 1 либо `null`;
- `stability` — устойчивость результатов от 0 до 1 либо `null`;
- количество всех, независимых, подтверждающих и противоречащих попыток;
- дата последнего evidence и последней измеренной проверки;
- дата следующего повторения;
- признак необходимости повторения;
- дата последнего подтверждения преподавателем;
- сводка источников;
- версия формулы и объяснение.

`null` у скорости или стабильности означает «данных недостаточно», а не нулевой результат.

## Прозрачный вес evidence

Для каждой попытки вычисляется эффективный вес:

```text
effectiveWeight =
  baseSkillWeight
  × sourceFactor
  × independenceFactor
  × recencyFactor
  × difficultyFactor
  × selfConfidenceFactor
  × errorTypeFactor
  × teacherFactor
```

### Роль навыка

Базовый вес приходит из `TaskSkill`:

- основной навык — обычно `1`;
- дополнительный — `0,65`;
- обязательная предпосылка — `0,45`.

Поэтому ошибка в многосоставной задаче не делает все связанные навыки одинаково слабыми.

### Источник

- полный вариант — `1`;
- адаптивная задача — `1,1`;
- короткий теоретический вопрос — `0,7`;
- ручная проверка — `1,2`;
- домашняя работа — `0,85`;
- контрольная — `1`;
- пробник — `1,1`;
- наблюдение на занятии — `0,75`;
- отдельное подтверждение преподавателя — `1,3`;
- самооценка «не изучал» не участвует в mastery.

### Самостоятельность

- самостоятельно — `1`;
- неизвестно — `0,85`;
- небольшая подсказка — `0,75`;
- существенная помощь — `0,45`.

Правильное решение с большой помощью остаётся положительным evidence, но не равно самостоятельному владению.

### Сложность

Успех в сложной задаче весит больше. Ошибка в сложной задаче штрафует меньше, чем ошибка в базовой. Значение ограничено уровнями 1–3.

### Давность

Используется плавное экспоненциальное снижение веса с периодом полураспада 180 дней и нижней границей `0,35`. Старый результат не исчезает, но свежие проверки влияют сильнее.

### Уверенность ученика

- правильный ответ при низкой уверенности получает коэффициент `0,85`;
- неправильный ответ при высокой уверенности — `1,1`;
- остальные случаи — `1`.

Это не заменяет проверку на угадывание этапа 2, а дополняет её.

### Тип ошибки

Концептуальные, логические ошибки и ошибки моделирования сильнее относятся к владению навыком. Чисто вычислительные, оформительские ошибки и неверное чтение условия получают пониженный отрицательный вес.

### Подтверждение преподавателем

Ручная проверка создаёт evidence с `teacherConfirmed = true` и дополнительным коэффициентом `1,15`. Она повышает уверенность, но не переписывает остальные результаты.

## Уровень владения

Используется сглаженная beta-модель с нейтральным prior:

```text
alpha = 1 + Σ(effectiveWeight × score)
beta  = 1 + Σ(effectiveWeight × (1 - score))
mastery = alpha / (alpha + beta)
```

Prior не позволяет одному ответу сразу дать 0% или 100%.

## Уверенность системы

Уверенность рассчитывается отдельно от mastery и учитывает:

- суммарный эффективный вес;
- количество независимых попыток;
- разнообразие источников;
- наличие ручного подтверждения.

Для содержательного статуса нужны минимум две независимые попытки и confidence не ниже `0,45`.

## Скорость

Скорость считается только по попыткам с результатом не ниже `0,6`. Ошибочный быстрый ответ не считается быстрым применением навыка.

```text
speed = clamp(1,2 - 0,3 × activeSeconds / expectedSeconds, 0,2, 1)
```

Итог — взвешенное среднее по подходящим попыткам.

## Стабильность

Стабильность появляется минимум после двух независимых попыток. Она основана на разбросе результатов и повышается по мере накопления до четырёх независимых подтверждений.

Серия `1, 1, 1, 1` стабильнее серии `1, 0, 1, 0`, даже если среднее значение выглядит приемлемым.

## Статусы

Статусы выбираются в следующем порядке:

1. `UNSTUDIED` — есть только самооценка «ещё не изучалось».
2. `INSUFFICIENT_DATA` — меньше двух независимых попыток или confidence ниже `0,45`.
3. `NEEDS_REVIEW` — навык был подтверждён, но наступил срок повторения.
4. `TEACHER_CONFIRMED` — свежая ручная проверка, mastery не ниже `0,65` и после неё нет противоречащей попытки.
5. `WEAK` — mastery ниже `0,45`.
6. `LEARNING` — mastery от `0,45` до `0,65`.
7. `NEEDS_REINFORCEMENT` — mastery ниже `0,8` или stability ниже `0,7`.
8. `MASTERED` — mastery не ниже `0,8` и стабильность не ниже `0,7`.

В API каждому статусу соответствует русское название.

## Повторение

Дата повторения считается от последней измеренной проверки:

- 90 дней для mastery от `0,85` и stability от `0,8`;
- 45 дней для mastery от `0,75` и stability от `0,6`;
- 21 день для остальных подтверждённых навыков с mastery от `0,6`.

Алгоритм этапа 4 использует `reviewDueAt` при формировании отдельного модуля повторения. Сама интерактивная дорожная карта здесь не строится.

## Объяснение для преподавателя

Каждый `StudentSkillState` содержит:

- версию формулы;
- значения prior;
- эффективный вес;
- средние применённые коэффициенты;
- пороги статуса;
- текстовые причины статуса;
- количество evidence по каждому источнику, их вес и последнюю дату.

Это позволяет ответить не только «какой статус», но и «почему система так решила».

## API

Ученик:

- `GET /diagnostics/profile/current`.

Преподаватель:

- `GET /teacher/diagnostics/students/:studentId/profile`.

Преподаватель может получить профиль только ученика своего класса. При первом чтении старой проекции она автоматически пересчитывается формулой `knowledge-profile-v1`.

## Проверочные сценарии

Автоматические тесты покрывают вымышленных учеников:

| Сценарий                                     | Ожидаемый результат                                       |
| -------------------------------------------- | --------------------------------------------------------- |
| Один правильный ответ                        | Недостаточно данных                                       |
| Только отметка «не изучал»                   | Ещё не изучалось                                          |
| Четыре независимые концептуальные ошибки     | Слабый навык                                              |
| Шесть стабильных самостоятельных успехов     | Освоено                                                   |
| Смешанная серия успехов и ошибок             | Требует закрепления                                       |
| Старые устойчивые успехи без свежей проверки | Требует повторения                                        |
| Свежая ручная проверка преподавателем        | Подтверждено преподавателем                               |
| Успехи с существенной помощью                | Ниже mastery и confidence, чем при самостоятельной работе |

## Ограничения

- Скорость зависит от качества клиентского учёта активного времени.
- Evidence будущих домашних работ, контрольных и занятий поддержано моделью, но их автоматическое добавление будет подключаться отдельными интеграциями.
- Интервалы повторения являются первой детерминированной политикой и должны калиброваться на реальных данных.
- Формула не заменяет решение преподавателя: все коэффициенты и причины доступны для проверки.
