# Персональная траектория подготовки: архитектура v1

## Граница текущего этапа

На этом этапе изучены текущая архитектура ExamClass и 18 PDF с теоретическими материалами. Созданы:

- нормализованный черновик карты навыков;
- зависимости между фундаментальными и сложными навыками;
- привязка страниц материалов к навыкам;
- схема интеграции с текущим backend.

Диагностика, расчёт маршрута, интерфейс дорожной карты и миграции базы данных на этом этапе не реализуются.

## Что уже есть в проекте

В текущей Prisma-схеме уже хорошо разделены пользователи, предметы, классы, темы, задачи, домашние задания и отправленные решения. `Topic` и `TopicPrerequisite` подходят для навигации по банку задач и грубых связей между темами.

Для персональной траектории этого недостаточно:

- задача сейчас относится только к одной теме, хотя проверяет несколько навыков;
- тема ЕГЭ описывает место задачи в каталоге, но не атомарное умение ученика;
- нет накопительных доказательств знания;
- нет уверенности оценки и числа независимых подтверждений;
- нет отдельной текущей проекции знаний, истории её изменений, маршрута и расписания повторений.

Поэтому существующие модели не заменяются. Рядом с ними добавляется слой навыков и обучения.

## Термины и границы данных

| Сущность | Что хранит | Чего не хранит |
| --- | --- | --- |
| Карта знаний | Все навыки предмета и зависимости между ними | Состояние конкретного ученика |
| Профиль знаний | Текущую оценку каждого навыка ученика | Порядок будущего обучения |
| Учебный маршрут | Упорядоченный план изучения и причины приоритета | Полную историю попыток |
| История изменений | Неизменяемые события, из которых получено текущее состояние | Удобную для UI агрегированную оценку |
| Расписание повторений | Когда и что нужно повторить | Общую структуру предмета |

`Topic` остаётся каталогом ЕГЭ. `Skill` становится единицей диагностики и обучения.

## Результат анализа материалов

Названия PDF нельзя использовать как таксономию. В материалах к одному номеру ЕГЭ встречаются навыки, необходимые в других номерах:

- степени, корни, формулы сокращённого умножения и квадратные уравнения повторяются в материалах к №6, №7, №9, №13 и №15;
- планиметрия является основой и для №1, и для сложных задач №14 и №17;
- №10 требует не только шаблонов текстовых задач, но и пропорций, уравнений, прогрессий и оценки результата;
- №14 опирается на планиметрию, векторы и отдельные пространственные отношения;
- №18 не является отдельной «темой»: задачи с параметром требуют алгебры, функций, неравенств и разбора случаев.

Машинно-читаемый черновик находится в `knowledge-map.v1.json`. Привязка материалов сделана по страницам, а не только по имени файла. В дальнейшем один материал можно будет дробить на более точные сегменты.

## Предлагаемые модели Prisma

### Полная карта знаний

`Skill`

- `id`, `subjectId`, `parentId`;
- стабильный `code`, название, описание;
- уровень: `FOUNDATION`, `CORE`, `ADVANCED`;
- состояние публикации и версия карты.

`SkillPrerequisite`

- `skillId`, `prerequisiteId`;
- минимально допустимое освоение;
- вес зависимости;
- тип связи: обязательная или усиливающая.

`TaskSkill`

- `taskId`, `skillId`;
- роль: основной навык, дополнительный или предварительный;
- вес доказательства;
- диагностическая различающая способность.

Это снимает ограничение «одна задача — одна тема», не меняя существующую связь `Task -> Topic`.

### Учебные материалы

`LearningMaterial`

- исходное имя, тип, версия, контрольная сумма;
- метаданные происхождения;
- статус ручной проверки.

`LearningMaterialSegment`

- материал, страницы и при необходимости координаты фрагмента;
- нормализованный заголовок;
- извлечённый текст или ссылка на хранилище.

`LearningMaterialSkill`

- сегмент, навык, роль и вес связи;
- источник разметки: вручную или предварительно AI;
- признак подтверждения преподавателем.

AI может предложить разметку, но рабочей она становится только после детерминированной валидации и/или подтверждения.

### Цели и диагностика

`StudentLearningGoal`

- ученик, предмет, целевой балл, дата экзамена;
- минут в неделю и доступные дни;
- дата создания и активность.

`AssessmentSession`

- тип: стартовая диагностика, уточнение, пробник, контрольная;
- ученик, версия алгоритма, начало и завершение.

`AssessmentAttempt`

- задача или теоретический вопрос;
- ответ, результат, затраченное время, подсказки;
- контекст предъявления и порядковый номер.

`SkillEvidence`

- ученик, навык, попытка;
- результат от `0` до `1`, вес, тип ошибки;
- источник: диагностика, домашняя работа, урок, контрольная, решение преподавателя;
- время и версия расчёта.

`SkillEvidence` — неизменяемый журнал. Исправление создаёт компенсирующее событие, а не переписывает прошлое.

### Профиль знаний

`StudentSkillState`

- `studentId`, `skillId`;
- `mastery`, `confidence`;
- общий эффективный вес и количество независимых задач;
- статус: неизвестен, пробел, изучается, освоен, пора повторить;
- время последнего доказательства и версия проекции.

Это быстрая проекция для интерфейса. Её всегда можно пересчитать из `SkillEvidence`.

### Маршрут, история и повторения

`LearningRoute`

- ученик, цель, версия карты знаний, статус;
- автор последнего решения: система или преподаватель.

`LearningRouteItem`

- навык, позиция, состояние;
- рассчитанный приоритет;
- объяснимые причины приоритета в структурированном JSON;
- плановые минуты и блокирующие предпосылки.

`LearningRouteEvent`

- добавление, перемещение, блокировка, подтверждение, завершение;
- автор, причина, старое и новое значение.

`ReviewSchedule`

- ученик, навык;
- дата следующего повторения;
- интервал, ступень, последний результат;
- версия правила повторения.

## Предсказуемая оценка знаний

Для каждого навыка хранится не один процент, а накопленная пара параметров:

```text
alpha = priorAlpha + Σ(weight × score)
beta  = priorBeta  + Σ(weight × (1 - score))
mastery = alpha / (alpha + beta)
```

`score` находится в диапазоне от `0` до `1`. `weight` зависит от связи задачи с навыком, самостоятельности решения и качества диагностического вопроса. Все веса и версии правил фиксируются в базе.

Уверенность считается отдельно и растёт только при разнообразных подтверждениях:

```text
confidence =
  evidenceSaturation
  × distinctTaskFactor
  × sourceDiversityFactor
  × recencyFactor
```

Начальные пороги для калибровки:

- меньше двух независимых подтверждений — статус остаётся `UNKNOWN`;
- `GAP`: `mastery < 0.55` и `confidence >= 0.55`;
- `MASTERED`: `mastery >= 0.80`, `confidence >= 0.70` и не меньше трёх разных задач;
- просроченное повторение переводит освоенный навык в `REVIEW_DUE`, не стирая историю.

Пороговые значения являются версионируемой конфигурацией, а не скрытой логикой LLM.

## Как уточнять причину ошибки

После полного варианта система не повторяет исходную задачу. Для каждого неуспеха она строит несколько проверяемых гипотез:

1. ошибка в основном навыке задачи;
2. ошибка в обязательной предпосылке;
3. вычислительная ошибка;
4. неверный перевод условия в модель;
5. случайная ошибка или нехватка времени.

Короткий адаптивный блок выбирает вопросы, которые лучше всего разделяют эти гипотезы. Например, после ошибки в логарифмическом уравнении можно дать:

- короткий вопрос на область допустимых значений;
- преобразование одного логарифма без решения уравнения;
- простое квадратное уравнение, возникающее после замены;
- объяснение, почему конкретный корень нужно исключить.

Правила выбора, остановки и веса результатов должны быть детерминированными. AI допустим для черновика формулировки объяснения или классификации свободного ответа, но итоговое изменение профиля проходит через проверяемые правила.

## Приоритет маршрута

Сначала исключаются навыки, заблокированные неосвоенными обязательными предпосылками. Для остальных считается объяснимый приоритет:

```text
priority =
  knowledgeGap
  × targetScoreRelevance
  × examFrequency
  × prerequisiteReach
  × deadlineUrgency
  × confidenceNeed
  + reviewDueBonus
```

`prerequisiteReach` повышает фундаментальные навыки, от которых зависит много последующих узлов. Поэтому пробел в дробях или степенях будет выше сложного логарифмического уравнения, даже если последнее встретилось в диагностике напрямую.

Каждый элемент маршрута хранит факторы, сформировавшие приоритет. Преподаватель видит объяснение и может подтвердить, переместить, временно заблокировать или заменить элемент. Его действие записывается в историю.

## Повторения

Начальная детерминированная сетка интервалов: `2, 7, 21, 45, 90` дней.

- успешное независимое воспроизведение переводит навык на следующую ступень;
- частичный успех сохраняет ступень;
- существенная ошибка сокращает интервал;
- доказательства из разных типов активности повышают уверенность сильнее повторов одной задачи.

После накопления реальных данных интервалы можно калибровать, не меняя формат хранения.

## Интеграция с текущим backend

Новые модели добавляются отдельной миграцией. Существующие `Topic`, `Task`, `HomeworkAssignment` и `HomeworkSubmission` остаются источниками событий:

- задача связывается с несколькими навыками через `TaskSkill`;
- ответ в диагностике или домашней работе создаёт `SkillEvidence`;
- преподавательская проверка ответа второй части создаёт evidence только после выставления результата;
- календарь использует `ReviewSchedule` и элементы маршрута наряду с дедлайнами ДЗ.

Для backend логично добавить независимые модули:

- `knowledge-map` — карта и зависимости;
- `diagnostics` — сессии, попытки и уточняющий блок;
- `student-model` — evidence и пересчёт профиля;
- `learning-route` — приоритеты, история и повторения.

На frontend TanStack Query хранит серверное состояние карты и профиля. Zustand нужен только для локального состояния интерактивной карты: масштаб, положение, выбранный узел, фильтры и режим редактирования. Данные знаний не дублируются в Zustand.

## Следующий отдельный этап

Добавить Prisma-модели полной карты знаний и материалов, миграцию и начальный seed из `knowledge-map.v1.json`. На этом этапе ещё не требуется строить диагностику или интерфейс маршрута.
